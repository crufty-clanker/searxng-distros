# Podman Compose file for Devtools SearXNG distro
# Run with: podman compose up -d
# Access at: http://localhost:8080

name: searxng-devtools

services:
  searxng:
    build:
      context: .
      dockerfile: Containerfile
    image: searxng/devtools:latest
    container_name: searxng-devtools
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./settings.yml:/etc/searxng/settings.yml:Z
      - ./limiter.toml:/etc/searxng/limiter.toml:Z
      - devtools-data:/var/cache/searxng/
    depends_on:
      - valkey
    networks:
      - searxng-net

  valkey:
    image: docker.io/valkey/valkey:9-alpine
    container_name: searxng-devtools-valkey
    restart: unless-stopped
    command: valkey-server --save 30 1 --loglevel warning
    volumes:
      - valkey-data:/data/
    networks:
      - searxng-net

volumes:
  devtools-data:
  valkey-data:

networks:
  searxng-net:
    driver: bridge
