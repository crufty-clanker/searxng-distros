# Auto-generated by scripts/regenerate-dependabot.sh — do not edit by hand.
# Regenerate after adding a new distro:  ./scripts/regenerate-dependabot.sh
version: 2
updates:

  # ── GitHub Actions (root) ──────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"

  # ── Distro subdirs ─────────────────────────────────────────────────

  - package-ecosystem: "docker"
    directory: "/distros/academia"
    schedule:
      interval: "weekly"
    groups:
      distros-academia-distro:
        patterns: ["*"]

  - package-ecosystem: "docker"
    directory: "/distros/devtools"
    schedule:
      interval: "weekly"
    groups:
      distros-devtools-distro:
        patterns: ["*"]

  - package-ecosystem: "docker"
    directory: "/distros/news"
    schedule:
      interval: "weekly"
    groups:
      distros-news-distro:
        patterns: ["*"]
